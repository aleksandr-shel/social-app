import { AnyAction, ThunkAction } from "@reduxjs/toolkit";
import { RootState } from "../store";



export const getProfiles = ():ThunkAction<void, RootState, unknown, AnyAction>=>{
    return async(dispatch)=>{

    }
}